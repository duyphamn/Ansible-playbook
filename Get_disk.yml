---
- hosts: all
  tasks:
          - name: Disk usage from command module
            command: df -h
            register: space
          - debug:
                  var: space.stdout_lines
            
          - name : Ping Server
            win_ping:
            when: ACTION == "ping"
